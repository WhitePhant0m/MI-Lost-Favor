ServerEvents.recipes(event => {
    
    // event.remove({output: [
    // ]})
    event.replaceInput( { input: 'minecraft:fire_charge', mod: 'magnumtorch' },'minecraft:fire_charge','hexerei:dried_sage_bundle')
    
})